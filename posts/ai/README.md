# AI posts

This folder contains AI-related blog posts (e.g., generative AI, foundation models, fine-tuning, ethics). These files are rendered by the site via the `posts/data/blogs.json` index; move or rename files carefully to avoid breaking links.